using UnityEngine;
using System.Collections;

namespace Prototypes.Ingame
{

    /// <summary>
    /// the basic brick can be placed in level, gets destroyed on collision with the ball (maybe also other things)
    /// and gives points to the gamemanager on destroy
    /// </summary>

    public class Brick : MonoBehaviour
    {

        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}