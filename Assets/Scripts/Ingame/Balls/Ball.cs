using UnityEngine;
using System.Collections;

namespace Prototypes.Ingame
{
    /// <summary>
    /// basic ball gets spawned on input if there is no other ball,
    /// bounces back if hit a brick, paddle or position is too close to the border
    /// </summary>

    public class Ball : MonoBehaviour
    {

        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
